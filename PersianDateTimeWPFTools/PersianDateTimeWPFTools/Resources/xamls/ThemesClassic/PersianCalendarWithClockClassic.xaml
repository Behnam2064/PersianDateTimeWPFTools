<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:o="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
    xmlns:primitives="clr-namespace:PersianDateTimeWPFTools.Windows.Controls.Primitives"
    xmlns:tools="clr-namespace:PersianDateTimeWPFTools.Tools"
    xmlns:pdtwt="clr-namespace:PersianDateTimeWPFTools.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary
            Source="pack://application:,,,/PersianDateTimeWPFTools;component/Resources/PredefiendDictionary.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Dark Classic -->
    <SolidColorBrush x:Key="PersianCalendarWithClockNormalForegroundDarkClassic" Color="#FFD0D0D0" />
    <SolidColorBrush x:Key="PersianCalendarWithClockBackgroundDarkClassic" Color="#FF202020" />
    <SolidColorBrush x:Key="PersianCalendarWithClockBorderBrushDarkClassic" Color="#FF555555" />

    <Style x:Key="PersianCalendarWithClockDarkClassic"
           TargetType="{x:Type pdtwt:PersianCalendarWithClock}">
        <Setter Property="UIElement.Focusable" Value="False" />
        <Setter Property="Control.Foreground"
                Value="{DynamicResource PersianCalendarWithClockNormalForegroundDarkClassic}" />
        <Setter Property="Control.Background"
                Value="{DynamicResource PersianCalendarWithClockBackgroundDarkClassic}" />
        <Setter Property="Control.BorderBrush"
                Value="{DynamicResource PersianCalendarWithClockBorderBrushDarkClassic}" />
        <Setter Property="tools:TitleElement.Background"
                Value="{DynamicResource PersianCalendarWithClockBackgroundDarkClassic}" />
        <Setter Property="Control.BorderThickness" Value="2" />
        <Setter Property="pdtwt:PersianCalendarWithClock.CalendarStyle"
                Value="{DynamicResource PersianCalendarDarkClassic}" />
        <Setter Property="pdtwt:PersianCalendarWithClock.ClockStyle"
                Value="{DynamicResource ClockDarkClassic}" />
        <Setter Property="tools:BorderElement.CornerRadius" Value="6" />
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type pdtwt:PersianCalendarWithClock}">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border
                            BorderBrush="{TemplateBinding Control.BorderBrush}"
                            BorderThickness="{TemplateBinding Control.BorderThickness}"
                            Background="{TemplateBinding Control.Background}"
                            CornerRadius="{Binding Path=(tools:BorderElement.CornerRadius),
                                RelativeSource={RelativeSource TemplatedParent}}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter
                                x:Name="PART_CalendarPresenter"
                                Grid.Column="0"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Height="{Binding ElementName=PART_ClockPresenter,
                                    Path=ActualHeight, Mode=OneWay,
                                    UpdateSourceTrigger=PropertyChanged}" />
                            <ContentPresenter
                                x:Name="PART_ClockPresenter"
                                Grid.Column="1"
                                Grid.Row="0"
                                Grid.RowSpan="2" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Light Classic -->
    <SolidColorBrush x:Key="PersianCalendarWithClockNormalForegroundLightClassic" Color="#FF555555" />
    <SolidColorBrush x:Key="PersianCalendarWithClockBackgroundLightClassic" Color="#FFF4F1EB" />
    <SolidColorBrush x:Key="PersianCalendarWithClockBorderBrushLightClassic" Color="#FFC8C1B5" />

    <Style x:Key="PersianCalendarWithClockLightClassic"
           TargetType="{x:Type pdtwt:PersianCalendarWithClock}">
        <Setter Property="UIElement.Focusable" Value="False" />
        <Setter Property="Control.Foreground"
                Value="{DynamicResource PersianCalendarWithClockNormalForegroundLightClassic}" />
        <Setter Property="Control.Background"
                Value="{DynamicResource PersianCalendarWithClockBackgroundLightClassic}" />
        <Setter Property="Control.BorderBrush"
                Value="{DynamicResource PersianCalendarWithClockBorderBrushLightClassic}" />
        <Setter Property="tools:TitleElement.Background"
                Value="{DynamicResource PersianCalendarWithClockBackgroundLightClassic}" />
        <Setter Property="Control.BorderThickness" Value="2" />
        <Setter Property="pdtwt:PersianCalendarWithClock.CalendarStyle"
                Value="{DynamicResource PersianCalendarLightClassic}" />
        <Setter Property="pdtwt:PersianCalendarWithClock.ClockStyle"
                Value="{DynamicResource ClockLightClassic}" />
        <Setter Property="tools:BorderElement.CornerRadius" Value="6" />
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type pdtwt:PersianCalendarWithClock}">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border
                            BorderBrush="{TemplateBinding Control.BorderBrush}"
                            BorderThickness="{TemplateBinding Control.BorderThickness}"
                            Background="{TemplateBinding Control.Background}"
                            CornerRadius="{Binding Path=(tools:BorderElement.CornerRadius),
                                RelativeSource={RelativeSource TemplatedParent}}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter
                                x:Name="PART_CalendarPresenter"
                                Grid.Column="0"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Height="{Binding ElementName=PART_ClockPresenter,
                                    Path=ActualHeight, Mode=OneWay,
                                    UpdateSourceTrigger=PropertyChanged}" />
                            <ContentPresenter
                                x:Name="PART_ClockPresenter"
                                Grid.Column="1"
                                Grid.Row="0"
                                Grid.RowSpan="2" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
